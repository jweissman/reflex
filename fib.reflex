class Algorithm {
    call(n) {
        self.compute(n)
    }
}

algorithm Fibonacci {
    compute(n) {
        // a=0;b=1;
        // test = n < 2;
        if (n<2) { 1 } else { self(n-1) + self(n-2) }
    }
}