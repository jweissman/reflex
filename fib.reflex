class Algorithm {
    call(n) {
        self.compute(n)
    }
}

algorithm Fibonacci {
    compute(n) {
        test = n <= 1;
        if (test) {
            1
        } else {
            self(n-1) + self(n-2) 
        }
    }
}