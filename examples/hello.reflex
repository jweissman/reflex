using 'paint'
paint = Paint.new()
class Greeter {
    init() {
        self.greeting = 'Hello, '
    }

    greet(subject) {
        self.greeting + paint.blue(subject) if subject
    }
}

greeter = Greeter.new()
greeter.greet("world")